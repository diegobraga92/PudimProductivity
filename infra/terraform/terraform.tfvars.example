# Copy to terraform.tfvars and fill in real values. `db_password` is sensitive
# — prefer setting it as TF_VAR_db_password or a secret store instead.
key_name          = "my-aws-keypair"
media_bucket_name = "pudimproductivity-media"
db_password       = "change-me"
