name: Setup Go

inputs:
  go-version-file:
    required: false
    default: backend/go.mod

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v7
      with:
        go-version-file: ${{ inputs.go-version-file }}
        cache: true
