{
  "name": "pudimproductivity-desktop",
  "private": true,
  "version": "0.0.1",
  "description": "PudimProductivity desktop app — Electron wrapper around the React web client",
  "author": {
    "name": "PudimProductivity contributors",
    "email": "pudim@localhost"
  },
  "license": "MIT",
  "homepage": "https://github.com/diegobraga92/PudimProductivity",
  "repository": {
    "type": "git",
    "url": "https://github.com/diegobraga92/PudimProductivity.git"
  },
  "main": "dist-electron/main.js",
  "scripts": {
    "build:web": "npm --prefix ../web run build:desktop",
    "build:shell": "tsc -p tsconfig.json",
    "icons": "node scripts/generate-icons.mjs",
    "dev": "npm run build:web && npm run build:shell && npm run icons && electron .",
    "package": "npm run build:web && npm run build:shell && npm run icons && node scripts/copy-web.mjs && electron-builder",
    "package:dir": "npm run build:web && npm run build:shell && npm run icons && node scripts/copy-web.mjs && electron-builder --dir",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^22.10.1",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "~5.6.2"
  }
}
